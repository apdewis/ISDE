# Diagnose
1. Run `git log --oneline -20` and `git diff HEAD~5` to see recent changes
2. Grep for the symptom across the repo before forming a hypothesis
3. State the suspected root cause in ONE sentence
4. Wait for user confirmation before editing any file
5. Do NOT refactor, extract helpers, or touch unrelated files
